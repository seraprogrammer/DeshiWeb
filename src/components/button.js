import "../assets/styles.css";
const Button = (props) => {
  return /*html*/ `
  <button $text="{counter} {counter <= 1 ? 'time' : 'times'}" class="button-45"></button>
  `;
};

export default Button;
